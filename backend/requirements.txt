fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
PyYAML>=6.0
httpx>=0.27
python-dotenv>=1.0
# Panchayat orchestration (optional — plain-asyncio fallback works without it)
langgraph>=0.2

# Dev / CI
pytest>=8.0
